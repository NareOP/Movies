import React from 'react';
import PropTypes from 'prop-types';

const MoviesList = () => <div>MoviesList Component</div>;

MoviesList.propTypes = {};

MoviesList.defaultProps = {};

export default MoviesList;
